namespace ReturnHome.Server.EntityObject.Spells
{
    //Enum to label what type a spell is for targeting purposes
    //Likely need more types later
    public enum SpellType : byte
    {
        Offensive = 0,
        Defensive = 1,
    }
}
