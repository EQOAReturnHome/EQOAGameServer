using ReturnHome.Server.Network;
using ReturnHome.Server.Opcodes.Messages.Server;

namespace ReturnHome.Server.Opcodes.Messages.Client
{
    public static class ClientAttack2
    {
        public static void ClientProcessAttack2(Session session, PacketMessage ClientPacket)
        {
            //ServerLoot.ServerLootOptions(session);
        }
    }
}
