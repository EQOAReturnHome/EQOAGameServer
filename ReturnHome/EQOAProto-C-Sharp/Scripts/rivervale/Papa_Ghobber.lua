function  event_say(choice)
diagOptions = {}
    npcDialogue = "Distribution is expensive with all of these new deputies we're hav'n to pay off. On top of that, the second family still has to pay respects to the Tanglefoots."
SendDialogue(mySession, npcDialogue, diagOptions)
end